import * as React from "react"
import { Heading, MenuButton } from 'theme-ui'

const IndexPage = () => {
  return (
    <main>
      <title>RECODE - Home</title>
    </main>
  )
}

export default IndexPage
